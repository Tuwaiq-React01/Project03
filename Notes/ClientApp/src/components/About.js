import React, { useState, useEffect } from 'react'

export default function About(props)
{
    return (
        <div>
            A note app, Why? because the note is all time best friend of humans
        </div>
    )
}